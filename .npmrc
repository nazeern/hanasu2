engine-strict=true

# Security: Only install packages released at least 7 days ago (10080 minutes)
# This protects against supply chain attacks like Shai-Hulud
# Packages can auto-update within semver ranges, but only after 7 days of maturity
minimum-release-age=10080
